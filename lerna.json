{
	"$schema": "https://json.schemastore.org/lerna.json",
	"npmClient": "yarn",
	"useWorkspaces": true,
	"packages": ["packages/*"],
	"version": "independent",
	"command": {
		"version": {
			"message": "chore(release): %s"
		}
	}
}
